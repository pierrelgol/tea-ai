[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"

[project]
name = "dataset-extractor"
version = "0.1.0"
description = "Extract and prepare datasets for tea-ai"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pipeline-config",
]

[project.scripts]
dataset-extractor = "dataset_extractor.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/dataset_extractor"]
